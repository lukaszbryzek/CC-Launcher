"""The terminal interface.

Nothing here is imported at module load by the layers below it: prompt_toolkit
is a heavy dependency and the configuration and vault layers must stay usable
without it.
"""
